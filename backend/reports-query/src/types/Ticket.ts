// Re-export canonical types from the single source of truth
export {
  Ticket,
  IncidentType,
  TicketStatus,
  TicketPriority,
  TicketPriority as Priority,
  PaginatedResponse,
  PaginationMetadata,
  TicketFilters,
} from './index';
